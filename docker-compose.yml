services:
  backend:
    build: .
    image: ${BACKEND_IMAGE:-readle-backend}:latest
    container_name: readle-backend
    ports:
      - "8080:8080"
    environment:
      - DB_URL=jdbc:postgresql://postgres:5432/readle_db
      - DB_USERNAME=admin
      - DB_PASSWORD=1234
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - readle-network

  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=readle_db
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - TZ=Asia/Seoul
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d readle_db" ]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - readle-network

networks:
  readle-network:
    driver: bridge

volumes:
  postgres-data:
